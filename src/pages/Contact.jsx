import { Mail, MapPin, Phone } from 'lucide-react'
import SectionHeader from '@/components/ui-custom/SectionHeader.jsx'
import { useLanguage } from '@/hooks/useLanguage'
export default function Contact(){const {t,isRTL}=useLanguage(); return <section className="section"><div className="container"><SectionHeader title={t('contact.title')} subtitle={t('contact.subtitle')} /><div className="split"><form className="card pad form"><input className="input" placeholder={t('contact.name')} /><input className="input" placeholder={t('contact.email')} /><input className="input" placeholder={t('contact.subject')} /><textarea className="input" placeholder={t('contact.message')} /><button className="btn btn-primary" type="button">{t('contact.send')}</button></form><div className="card pad"><div className="info-list"><div className="info-item"><Phone/> <div><strong>{t('contact.phone')}</strong><br/>+962 79 000 0000</div></div><div className="info-item"><Mail/> <div><strong>Email</strong><br/>info@foodloop.jo</div></div><div className="info-item"><MapPin/> <div><strong>{t('contact.location')}</strong><br/>{isRTL?'عمّان، الأردن':'Amman, Jordan'}</div></div></div></div></div></div></section>}
